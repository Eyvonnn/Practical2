package com.example.practical2


data class MyName (var name:String="", var nickname: String="") //use var because need edit later instead of using bar??

//constructure put value, cannot accept null so make empty =""